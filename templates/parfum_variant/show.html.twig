{% extends 'base.html.twig' %}

{% block title %}ParfumVariant{% endblock %}

{% block body %}
    <h1>ParfumVariant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ parfum_variant.id }}</td>
            </tr>
            <tr>
                <th>Sillage</th>
                <td>{{ parfum_variant.sillage }}</td>
            </tr>
            <tr>
                <th>Tenue</th>
                <td>{{ parfum_variant.tenue }}</td>
            </tr>
            <tr>
                <th>Size</th>
                <td>{{ parfum_variant.size }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ parfum_variant.image }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ parfum_variant.price }}</td>
            </tr>
            <tr>
                <th>Stock</th>
                <td>{{ parfum_variant.stock }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_parfum_variant_index') }}">back to list</a>

    <a href="{{ path('app_parfum_variant_edit', {'id': parfum_variant.id}) }}">edit</a>

    {{ include('parfum_variant/_delete_form.html.twig') }}
{% endblock %}
